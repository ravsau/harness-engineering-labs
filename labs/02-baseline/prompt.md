Read the baseline files. Run `sh init.sh`, add one task, list it, and report the
exact output. Add an init sanity line that reports the number of feature records.
Do not add dependencies or change the JSON schema. Leave the workspace runnable
for the next lesson.
