# Runbook

Run `sh init.sh` to validate the baseline. Add a task with
`python3 taskboard.py add "text" --file tasks.json`, then inspect it with
`python3 taskboard.py list --file tasks.json`. Run each lesson through the root
`lab.py` command with an explicit workspace.
