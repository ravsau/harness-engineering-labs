# One task

Inspect `features.json` and choose the first task whose `passes` value is false.
Work only on that task. Run `python3 taskboard.py list --file tasks.json`, then
run `python3 ../lab.py check 04 --workspace .` from inside `work/`. Return the
exact commands and outputs, then append the next step to the handoff. Stop if
the check fails or if a second task would be required.
